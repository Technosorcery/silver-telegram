# Database
DATABASE_URL=postgresql://silver_telegram_dev:silver_telegram_dev@localhost/silver_telegram_dev

# OIDC Configuration (use double underscore for nested fields)
OIDC__ISSUER_URL=https://your-oidc-provider.example.com/
OIDC__CLIENT_ID=your-client-id
OIDC__CLIENT_SECRET=your-client-secret
OIDC__REDIRECT_URI=http://localhost:3000/auth/callback

# Optional OIDC settings (defaults: scopes=openid,email,profile; groups_claim=groups; user_group=platform-users; admin_group=platform-admins)
# OIDC__GROUPS_CLAIM=groups
# OIDC__USER_GROUP=platform-users
# OIDC__ADMIN_GROUP=platform-admins

# Session settings
SESSION__SECURE_COOKIES=false  # Set to true for production (requires HTTPS)
# SESSION__DURATION_MINUTES=5
# SESSION__CLEANUP_INTERVAL_SECONDS=300
